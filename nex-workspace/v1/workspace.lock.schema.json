{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "Workspace Lock Configuration",
	"description": "Lock file that stores the current configuration for a project",
	"type": "object",
	"properties": {
		"projectName": {
			"type": "string",
			"description": "The name of the project type (defines the templates to use)"
		},
		"appName": {
			"type": "string",
			"description": "The name of the app"
		},
		"variantName": {
			"type": "string",
			"description": "The name of the currently in-use variant"
		},
		"prompts": {
			"type": "object",
			"description": "The prompts that were asked during creation of the project",
			"additionalProperties": {
				"oneOf": [
					{
						"type": "string",
						"description": "String prompt response"
					},
					{
						"type": "number",
						"description": "Numeric prompt response"
					},
					{
						"type": "boolean",
						"description": "Boolean prompt response"
					}
				]
			}
		}
	},
	"required": ["projectName", "appName", "variantName", "prompts"]
}
